<%= turbo_stream_action_tag(
  "append",
  target: "lines",
  template: %(#{render "lines", lines: @lines})
) %>
<%= turbo_stream_action_tag(
  "replace",
  target: "pager",
  template: %(#{render "pager", pagy: @pagy})
) %>
